strictness: medium
test-warnings: true
doc-warnings: false

ignore-paths:
  - venv

ignore-patterns:
  - (^|/)migrations(/|$)
  - (^|/)tests.py(/|$)

pylint:
  disable:
    - suppressed-message
    - useless-suppression

