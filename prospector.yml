uses:
    - django

ignore-patterns:
  - jbank/migrations/
  - jbank/tests.py

pylint:
  disable:
    - consider-using-f-string
    - imported-auth-user
    - useless-suppression
    - django-not-configured
    - broad-exception-raised
  load-plugins:
    - pylint-django
